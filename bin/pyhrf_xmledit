#! /bin/env python
from pyhrf.xmledit.domviewer import *

app = QtGui.QApplication(sys.argv)
if len(sys.argv) > 1:
    myapp = DomViewer(sys.argv[1])
else:
    myapp = DomViewer()
myapp.show()
sys.exit(app.exec_())
